
server:
  port: ${PORT:8080}

spring:
  datasource:
    url: ${DATABASE_URL}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:Pramodmsd@777}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: ${JWT_SECRET:change-this-to-a-long-random-secret-in-production}
  expiration-ms: 86400000   # 24 hours

razorpay:
  key-id: ${RAZORPAY_KEY_ID:rzp_test_xxxxxxxx}
  key-secret: ${RAZORPAY_KEY_SECRET:xxxxxxxxxxxxxxxx}

cloudinary:
  url: ${CLOUDINARY_URL:cloudinary://<api_key>:<api_secret>@<cloud_name>}

cors:
  allowed-origins: ${ALLOWED_ORIGINS:http://localhost:5173}
