server:
  port: ${PORT:8080}

spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://ep-lucky-frost-axddvb9u-pooler.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require}
    username: ${DB_USER:neondb_owner}
    password: ${DB_PASSWORD:npg_LeJujrVbRz14}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: ${SriBalajiPG@2026#JWT$Secret!1234567}
  expiration-ms: 86400000

razorpay:
  key-id: ${RAZORPAY_KEY_ID}
  key-secret: ${RAZORPAY_KEY_SECRET}

cloudinary:
  url: ${CLOUDINARY_URL}

cors:
  allowed-origins: ${ALLOWED_ORIGINS}
