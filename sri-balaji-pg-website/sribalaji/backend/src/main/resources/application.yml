server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sribalaji_pg?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Pramodmsd@777
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: ${JWT_SECRET:local-dev-secret-change-in-production}
  expiration-ms: 86400000

razorpay:
  key-id: ${RAZORPAY_KEY_ID:rzp_test_placeholder}
  key-secret: ${RAZORPAY_KEY_SECRET:placeholder_secret}

cloudinary:
  url: ${CLOUDINARY_URL:cloudinary://placeholder}

cors:
  allowed-origins: http://localhost:5500,http://127.0.0.1:5500,file://